# TCP Congestion Control Simulator
A simulation of three TCP congestion control algorithms built with Python and Tkinter.

## Algorithms
1. **Reno** — classic AIMD, reacts to packet loss
2.  **Cubic** — uses a cubic growth function, better on fast networks
3. **Vegas** — watches RTT instead of waiting for loss

## How to Run
1. Python Version 
2. Install the one dependency: pip install matplotlib
3. Then Run: python main.py
